<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WalkAware - <%= currentPage %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/style.css">

</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">

      <a class="navbar-brand" href="/">WalkAware</a>
      <% if(typeof nome !=='undefined' ){%>

        <% if (currentPage !=='login' ) { %>
          <!-- Menù schermo piccolo -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <!-- Menù normale -->
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav nav-underline me-auto mb-lg-0">
              <li class="nav-item">
                <a class="nav-link <%= currentPage === 'segnalazioni' ? 'active' : '' %>"
                  href="/segnalazioni">Segnalazioni</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= currentPage === 'aziende' ? 'active' : '' %>" href="/aziende">Aziende</a>
              </li>
              <% if(typeof isSupportoTecnico !=='undefined' && isSupportoTecnico==='true' ){%>
                <li class="nav-item">
                  <a class="nav-link <%= currentPage === 'registrazione' ? 'active' : '' %>" href="/utente">Aggiungi
                    Utente</a>
                </li>
                <% }%>
            </ul>
            <!-- Dettagli utente -->
            <button type="button" class="btn btn-outline-primary" data-bs-toggle="modal"
              data-bs-target="#userDetailsModal">
              <i class="fa-solid fa-user me-2"></i> Ciao, <%= nome%>!
            </button>
            <% }%>
          </div>
          <% } %>
    </div>
  </nav>

  <!-- Modal informazioni utente -->
  <% if(typeof nome !=='undefined' ){%>
    <div class="modal fade" id="userDetailsModal" tabindex="-1" role="dialog" aria-labelledby="userDetailsModalLabel"
      aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="userDetailsModalLabel"><i class="fa-solid fa-user me-2"></i>Dettagli utente</h5>
          </div>
          <div class="modal-body">
            <span>
              <strong>Nome:</strong>
              <%= nome%> <br>
            </span>
            <span>
              <strong>Email:</strong>
              <%= email%> <br>
            </span>

            <% if (typeof isSupportoTecnico !=='undefined' && isSupportoTecnico==='true' ){ %>
              <span>
                <strong>Supporto tecnico</strong>
              </span>
              <% } %>
          </div>
          <div class="modal-footer">
            <a href="/utente/logout" class="btn btn-outline-danger me-2 ">
              <i class="fa-solid fa-right-from-bracket me-2"></i>Logout</a>

            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annulla</button>

            <!-- <button type="button" class="btn btn-primary" data-bs-target="#utente-modifica"
            data-bs-toggle="modal">Modifica</button> -->
            <a href="/utente/modifica" class="btn btn-primary"><i class="fa-solid fa-pencil me-2"></i>Modifica</a>
          </div>
        </div>
      </div>
    </div>
    <% }%>



      <div class="container">
        <div class="row justify-content-center mt-5">