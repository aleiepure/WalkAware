<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WalkAware - <%= currentPage %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/style.css">

</head>

<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">WalkAware</a>
            <% if (currentPage !=='login' && currentPage !=='login_aziende' && currentPage !=='valida_buono' ) { %>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav nav-underline me-auto mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link <%= currentPage === 'segnalazioni' ? 'active' : '' %>"
                                href="/segnalazioni">Segnalazioni</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= currentPage === 'aziende' ? 'active' : '' %>"
                                href="/aziende">Aziende</a>
                        </li>
                        <% if(typeof isSupportoTecnico !== 'undefined' && isSupportoTecnico === 'true'){%>
                        <li class="nav-item">
                            <a class="nav-link <%= currentPage === 'registrazione' ? 'active' : '' %>"
                                href="/registrazione">Aggiungi Utente</a>
                        </li>
                        <% }%>

                    </ul>
                    <div class="d-flex">
                        <a href="/utente/logout" class="btn btn-outline-primary">
                            <i class="fa-solid fa-right-from-bracket me-2"></i>Logout</a>
                    </div>
                </div>
                <% } %>
    </nav>
